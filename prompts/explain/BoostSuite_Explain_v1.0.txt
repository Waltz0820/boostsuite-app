【Stage3｜Explain Layer v1.1】
目的：Stage2の出力に対し、「どこを・どう強化したか」をユーザー目線で簡潔に注釈化する。
出力形式：**JSON**のみ（オブジェクト）。配列ではなく { "annotations": [...] } のトップレベルで返すこと。

制約:
- 件数: 8〜12件
- 1件 = { section, text, type, importance, [quote], [before], [after], [tip] }
- section ∈ { "titles","lead","bullet_1","bullet_2","bullet_3","bullet_4","bullet_5","diff","scene","notice","closing","qa","sns" }
- type ∈ { "Warmflow","FactLock","SEO","Emotion","FutureFlow","Structure","Humanize" }
- importance ∈ { "high","medium","low" }
- text はユーザー語彙（専門用語は避ける／20〜80字目安）
- quote は Stage2本文から短い引用（任意、30字以内）。before/after は改善の対比（任意）。
- 冗長な重複や同義反復は避ける。必ずセクションを分散させる（同じsectionに偏らせない）。

評価観点:
- Warmflow（導入→体験→余韻）を保ちながら、事実・但し書きの保持（景表法/薬機法）。
- SmartBullet（1〜4機能/5情緒）の役割が保たれているか。
- タイトルの検索語（500mL/USB/調光等）が残っているか（SEO）。
- クロージングに所有後の未来/習慣が入っているか。

— Stage2出力 —
{{STAGE2_TEXT}}
